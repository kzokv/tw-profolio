import type { RoundingMode } from "./types.js";
export declare function applyRounding(value: number, mode: RoundingMode): number;
export declare function bpsAmount(baseNtd: number, bps: number): number;
